# Implementing Search/Limits/Pagination on backend and Debounced Search in React

Since advocates might be a very large number we would need to implement limits and pagination on the backend. If this is implemented we would need to debounce the search as the user is typing so we also need to do these changes on the frontend to call the api only after 500s after the user stops typing.

---

# Update Next version to latest

The Next.js version should be updated to 15, errors were showing nextjs 14 is outdated.